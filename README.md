# didactic-spoon

build for development/debug with
```
cmake --preset=debug
cmake --build build
```
build for running
```
cmake --preset=release
cmake --build build
```

presets are set up to use Ninja
